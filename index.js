var config = {
    autoplay: true,
    autoplaySpeed: 3500,
    buttonAfterId: "buttonAfter",
    buttonBeforeId: "buttonBefore",
    bulletMenuClass: "dot"
};

var carousel = mateCarousel ('inner', config);